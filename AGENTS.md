# K-Bap FE — 에이전트·리뷰 지침 (Codex 코드리뷰용)

이 레포는 React Native(Expo) 앱. PR 리뷰 시 아래 기준으로 코멘트해 주세요.

## lint 게이트 (2026-09-21, KB-602)

FE 발주 DoD의 "lint 통과" = **`npm run lint:changed` 종료 코드 0** — `origin/develop...HEAD`에서
**추가·수정된 줄**에 warning이 1건도 없어야 한다(대상 = eslint가 보는 소스 전부 — .ts/.tsx/.js/.jsx/.mjs/.cjs)(`react-hooks/*` 포함).
- **신규 파일은 전체가 추가된 줄** = 전건 검사가 된다.
- 기존 줄의 부채는 통과시킨다 — 파일 단위로 걸면 부채 파일을 한 줄만 건드려도 red라 게이트가
  상시 red가 된다(실제로 게이트 도입 커밋 자체가 걸렸다). **새 부채만 막는 게 래칫이다.**
- 삭제한 줄의 warning으로는 실패하지 않는다.

전체 `npm run lint`의 warning은 **기존 부채**다. eslint가 그동안 미설치라 한 번도 돌지 않았고,
켜자마자 `react-hooks/*` 위반 155건이 드러났다 — 끄지 않고 warn으로 내려 계속 보이게 두고
(`eslint.config.js` 래칫 블록) KB-603~으로 분할 소진한다. 소진된 룰은 그 블록에서 지워 error로 되돌린다.

## 리뷰 관점

1. **정확성**: 실제 버그·엣지 케이스·회귀 위험 우선. 스타일 지적은 동작에 영향 있을 때만.
2. **헌법 게이트** (위반 = 반드시 지적):
   - 유니코드 이모지 UI 사용 금지(아이콘은 SVG 벤더) — 예외: 맵기 🌶️(승인됨) · 국기 이모지(국적 표시 전 노출처 — 온보딩 국적 리스트·리뷰 작성자 국적 배지·프로필, 헌법 v2.3.0 · 승인됨 9/15) · 아기 👶(맵기 NONE·MILD '아이도 먹을 수 있음' 배지 한정, 헌법 v2.3.1)
   - 위험도 4상태(SAFE/CAUTION/DANGER/UNKNOWN)는 색+형태 병행 구분(색맹 접근성)
   - 위험도 판정 불확실 시 보수적 처리 — "안전 보장" 표현·false-safe 로직 금지
   - i18n: 사용자 노출 문자열은 하드코딩 금지, 10개 로케일 키 패리티 유지
   - reader(UI 언어) / place(사장님 확인 카드 = 한국어 상수) 언어 축 분리
3. **신규 한국어 카피**: diff에 새/변경된 ko.json 값이나 한글 리터럴이 있으면 말투 기준으로도 코멘트
   — 친근한 해요체, 대시(—) 부연 대신 문장 분리, 추상 동사("제공해요") 대신 구체 동사,
   안전을 확정·보장하는 표현 금지. (정본: spec 레포 bridge/codex/KOREAN-COPY-HANDOFF.md §5)
4. **채널 플래그**: `src/lib/flags.ts`의 채널 게이트(PROD_CHANNEL 기반) 변경은 스토어 유저 노출을
   바꾸는 고위험 변경 — 의도 확인 코멘트 필수.
5. **어댑터 격리**: 서버 와이어 형식 변환은 `src/lib/api/*Adapter.ts`에 격리하는 관례 — 화면
   컴포넌트에 와이어 파싱이 새로 들어오면 지적.
6. **사장님 카드**(`src/lib/order/orderCard.ts`): 한국어 문장이 조사 처리·안전 강등 규칙과 결합된
   안전 크리티컬 영역 — 문구 변경 PR은 유닛 동반 여부 확인.

## 하지 말 것

- 테스트를 약화시키는 수정 제안(단언 완화·스킵) 금지
- 리라이트 수준의 대안 코드 제안 자제 — 문제와 근거 중심으로
